- hosts: localhost
  tags:
    - always
  tasks:
    - name: "Add target host to ansible memory"
      add_host:
        name: "{{ ip }}"
        groups: myhost
        ansible_user: "{{ user_name }}"
    - name: "Install necessary galaxy roles"
      local_action: command ansible-galaxy install -r requirements.yml

#- hosts: 127.0.0.1
#  connection: local
- hosts: all
  vars:
    cli_packages: ['git', 'zsh', 'ansible', 'awscli', 'curl', 'packer', 'terraform-docs', 'terraform_landscape', 'warrensbox/tap/tfswitch', 'warrensbox/tap/tgswitch', 'kops', 'kubernetes-cli', 'kubernetes-helm', 'go', 'minikube', 'wget', 'openssl', 'ag', 'docker-compose']
    app_packages: ['1password', 'visual-studio-code', 'firefox', 'evernote', 'spotify', 'dropbox', 'virtualbox', 'docker', 'slack', 'notion']
  tasks:
    - import_role:
        name: base
      tags: ['packages']

    - import_role:
        name: shell
      tags: ['shell']

    - import_role:
        name: python
      tags: ['python']

    - import_role:
        name: java
      tags: ['java']

    - import_role:
        name: nodejs
      tags: ['nodejs']

    - import_role:
        name: ssh
      tags: ['ssh']

    - import_role:
        name: git
      tags: ['git']

    - import_role:
        name: vim
      tags: ['vim']
